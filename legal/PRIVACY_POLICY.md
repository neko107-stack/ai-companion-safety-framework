# プライバシーポリシー
### AIコンパニオンアプリ

最終更新日：2026年4月20日

---

## 1. はじめに

本プライバシーポリシーは、AIコンパニオンアプリ（以下「本アプリ」）における個人情報および会話データの取り扱いについて定めます。

本アプリを利用することで、本ポリシーの内容に同意したものとみなします。

---

## 2. 収集するデータと保存場所

### 2.1 ローカル保存データ（外部送信なし）

本アプリは、以下のデータを**ユーザーのデバイス内のみ**に保存します。外部サーバーへの送信は一切行いません。

| データ種別 | 保存形式 | 保存期間 |
|----------|---------|---------|
| 会話履歴 | JSON（localStorage）／設定で AES-256-GCM暗号化（オプトイン） | ユーザーが削除するまで |
| コンパニオン設定・人格 | JSON（localStorage）／設定で AES-256-GCM暗号化（オプトイン） | ユーザーが削除するまで |
| プロファイル・オンボーディング回答 | JSON（localStorage）／設定で AES-256-GCM暗号化（オプトイン） | ユーザーが削除するまで |
| 長期記憶サマリー | JSON（localStorage）／設定で AES-256-GCM暗号化（オプトイン） | ユーザーが削除するまで |
| アプリ設定（テーマ・音量等） | JSON（localStorage） | ユーザーが削除するまで |
| エラーログ | JSON（localStorage、最大200件） | ユーザーが削除するまで |
| 判断ログ（開発用・既定で無効） | JSON（localStorage、最大100ターン） | ユーザーが削除するまで |

> **保存時暗号化は既定では無効です。** 設定画面で有効化し PIN を設定した場合にのみ、上記4種（会話履歴・コンパニオン設定・プロファイル・長期記憶サマリー）が AES-256-GCM で暗号化されて保存されます。未設定の場合は平文の JSON として保存されます（`src/safety/secure-storage.js`）。アプリ設定・エラーログ・判断ログは暗号化の対象外です。

> **判断ログについて。** AIがどの判断で応答したかを開発者が追うための機能で、**既定では無効**です。有効な場合でも記録するのは判断のメタ情報のみで、**会話本文・AI応答本文・AIの思考本文・長期記憶の内容は記録しません**（記録されるのは文字数・危機レベル・会話モードの遷移・長期記憶の件数と確実性・使用エンジン名とレイテンシ）。

### 2.2 セッション内のみ保存されるデータ

以下のデータは、タブ・ブラウザを閉じると自動的に消去されます。

| データ種別 | 保存場所 |
|----------|---------|
| AIエンジンのAPIキー | sessionStorage |

### 2.3 保存場所（プラットフォーム別）

| プラットフォーム | 保存場所 |
|--------------|---------|
| Webブラウザ版 | ブラウザの localStorage / sessionStorage |
| Windows版 | `C:\Users\ユーザー名\AppData\Local\AICompanion\` |
| macOS版 | `~/Library/Application Support/AICompanion/` |
| iOS版 | アプリのサンドボックス内 |
| Android版 | `内部ストレージ/Android/data/com.aicompanion/files/` |

---

## 3. AIエンジンへのデータ送信

本アプリは、AIサービスに会話データを送信して応答を生成します。送信される内容には、会話本文のほか、システムプロンプトに含まれる**呼び名・興味の設定・長期記憶サマリー**が含まれます。

送信経路はプランによって2通りあります。

| プラン | 経路 |
|--------|------|
| 自分のAPIキーを使う場合 | ブラウザから各AIサービスへ直接送信されます |
| 無料プラン／サポータープラン | 運営者のサーバー（Vercel 上の `/api/chat`）を経由して各AIサービスへ送信されます。運営者のサーバーは中継のみを行い、会話データを保存しません |

| AIエンジン | 送信先 | プライバシーポリシー |
|-----------|--------|-----------------|
| Claude（Anthropic） | api.anthropic.com | [Anthropic Privacy Policy](https://www.anthropic.com/privacy) |
| GPT（OpenAI） | api.openai.com | [OpenAI Privacy Policy](https://openai.com/policies/privacy-policy) |
| Gemini（Google） | generativelanguage.googleapis.com | [Google Privacy Policy](https://policies.google.com/privacy) |
| Llama（Meta） | ローカル環境のみ | 外部送信なし |

**重要**: 各AIサービスへの会話データの取り扱いは、各社のプライバシーポリシーに準じます。特にプライバシーを重視する場合は、Llamaのローカルモードの使用を推奨します。

---

## 4. エラーログについて

エラーログには以下の情報**のみ**を記録します。

**含まれる情報（技術情報のみ）:**
- エラーコード・カテゴリ・タイムスタンプ
- 使用AIエンジン名・HTTPステータスコード
- アプリバージョン

**含まれない情報:**
- 会話内容
- APIキー
- ユーザー名・個人情報

---

## 5. 危機検知システムとデータ取り扱い

本アプリはC-SSRS（Columbia Suicide Severity Rating Scale）に基づく危機検知システムを搭載しています。

- 危機検知はデバイス内のみで処理されます
- 検知結果は外部に送信されません
- 危機レベルの遷移のみがエラーログに記録されます（会話内容は含まない）

---

## 6. データの削除

### 6.1 部分削除
設定画面 → データ管理 より、以下を個別に削除できます。
- 会話履歴（長期記憶サマリー・判断ログも同時に削除されます）
- エラーログ

### 6.2 完全削除（リセット）
設定画面 → 全データリセット から、本アプリに関するすべてのローカルデータを削除できます。

### 6.3 アプリ削除時
アプリをアンインストールすると、すべてのローカルデータが完全に消去されます。
**重要**: アンインストール前に必ずエクスポートをお取りください。

---

## 7. データのエクスポート・バックアップ

設定画面 → エクスポートから、AES-256-GCM暗号化されたバックアップファイルを作成できます。このファイルはユーザーが設定したパスワードで保護されます。

---

## 8. 未成年者の利用

本アプリは13歳未満の利用を想定していません。13歳未満の方が利用していることが判明した場合、保護者の方はブラウザのサイトデータ削除機能によりデータを削除してください。

---

## 9. ポリシーの変更

本ポリシーは予告なく変更される場合があります。重要な変更がある場合は、アプリ内またはGitHubリポジトリにて通知します。

---

## 10. お問い合わせ

本ポリシーに関するご質問は、GitHubのIssueよりお問い合わせください。

https://github.com/neko107-stack/ai-companion-safety-framework/issues

---

*本アプリはMITライセンスのもとで公開されているオープンソースソフトウェアです。*
